export interface ISubcomponentValue {
  id: string; //Id = Id el que se envía al editar la asignacion de cargos
  chargeId: number;
  chargeName: string;
  relativeWeight: number;
  subcomponentId: string;
  minimunPercentage: number;
  maximunPercentage: number;
}
